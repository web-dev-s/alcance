location / {
  if (!-e $request_filename){
    rewrite ^(.*)$ /index.html break;
  }
}
rewrite ^(.+)$ /index.html last;
server {
	listen 80 default_server;
	listen [::]:80 default_server;
	server_name myds.me confidential.myds.me;
	return 301 https://$server_name$request_uri;
}